{
  "name": "toastmasters",
  "private": true,
  "scripts": {
    "build": "gridsome build",
    "develop": "gridsome develop",
    "explore": "gridsome explore",
    "deploy": "gridsome build && firebase deploy",
    "deploy-netlify": "gridsome build && firebase deploy"
  },
  "dependencies": {
    "firebase": "^6.2.0",
    "gridsome": "^0.6.0"
  },
  "devDependencies": {
    "gridsome-plugin-tailwindcss": "^2.2.15"
  }
}
